{
  "name": "spreadsheet",
  "version": "1.0.0",
  "description": "TypeScript implementation of a spreadsheet.",
  "scripts": {
    "build": "tsc && cp lib/parser.js output/",
    "test": "tsc --outDir test_output tests/*.ts && node test_output/tests/*.js && echo Tests Pass"
  },
  "author": "vogtb <bvogt at gmail.com>",
  "license": "MIT",
  "dependencies": {
    "formulajs": "^1.0.8"
  }
}
