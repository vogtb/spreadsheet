{
  "name": "spreadsheet",
  "version": "1.0.0",
  "description": "TypeScript/javascript implementation of a spreadsheet.",
  "scripts": {
    "clean": "rm -rf output/* && rm -rf test_output/*",
    "build": "tsc && cp lib/parser.js output/",
    "test": "./tests.sh",
    "test:quiet": "./tests.sh | grep -v Test:"
  },
  "author": "vogtb <bvogt at gmail.com>",
  "license": "MIT",
  "dependencies": {
    "moment": "^2.17.1"
  }
}
